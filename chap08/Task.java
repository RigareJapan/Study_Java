public class Task {
    public static void main(String[] args) {
        // 魔術師クラス(Wizard)を作りましょう
        // 魔術師は名前、HP、MP、Levelというステータスを持ちます。
        // 魔術師は魔法(magic)と回復(heal)という行動ができます。
        // 魔法(magic)はLevel×5のダメージを与えることができます(void型でprintln()で表示させる)
        // 回復(heal)はLevel×3の回復をすることができます(void型でMPに変更を加えるprintln()で現在のMPを表示させる)

        // Wizard w = new Wizard();
        // w.hp = 10;
        // w.mp = 10;
        // w.level = 10;
        // w.magic();
        // w.heal();

        // 魔法(magic)は使用するごとにmpが5ずつ減少します。
        // mpが不足している場合(5未満)は"魔術師はMP不足だ！"と表示させてください。(if文を使いましょう)

        // Wizard w = new Wizard();
        // w.hp = 10;
        // w.mp = 3;
        // w.level = 10;
        // w.magic();

    }
}
